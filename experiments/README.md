Numbered in the order they were run (September 2026). Kept verbatim as the
record behind the numbers in the docs; they import from the old script
layout and are not maintained. The reproducible versions are the
`inkscript trace` and `inkscript alphabet` commands.

1. Page outlines and a per-page alphabet (97.4% ink reproduced; 310 shapes/735 blobs).
2. Alphabet across 20 pages: saturates within a document, resets between documents.
3. Scale-normalised, outline-matched matcher: the reset is a different type weight.
4. Label-once purity: strict groups 99.8% consistent, 36% of words covered.
5. First Type 3 page (page 1 only), superseded by `inkscript.pdf`.
